// GameAnalytics keys — EXAMPLE / committed documentation.
//
// To go live: copy this file to `ga-keys.local.js` (git-ignored) in this same
// folder and paste the Android Game Key + Secret Key from gameanalytics.com.
// With empty/absent keys the analytics wrapper stays in safe "tap-only" mode:
// nothing is sent, but every event is visible locally via window.__BOUNTU_GA_TAP.
//
// Shape: a `keys` export with non-empty `game` and `secret` strings.
export const keys = {
  game: '',
  secret: '',
};
