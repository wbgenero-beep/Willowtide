node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
npm-debug.log*

# GameAnalytics keys (never commit real keys)
src/ga-keys.local.js

# Android signing — keystore + credentials are human-owned, never committed
android/keystore.properties
*.keystore
*.jks

# TypeScript incremental build cache
*.tsbuildinfo
